# 贡献与 Git 协作规范

## 分支策略

仓库采用轻量主干开发：

- `main`：受保护的稳定主分支；
- `feature/<topic>`：新功能；
- `fix/<topic>`：缺陷修复；
- `refactor/<topic>`：不改变外部行为的重构；
- `docs/<topic>`：文档；
- `test/<topic>`：测试和测试资产；
- `chore/<topic>`：构建、依赖和工程维护。

分支名使用小写英文和连字符，例如：

```text
feature/filesystem-scanner
fix/sidecar-atomic-write
docs/obsidian-protocol
```

## 提交规范

提交信息采用 Conventional Commits：

```text
<type>(<scope>): <summary>
```

常用类型：

- `feat`：新功能；
- `fix`：缺陷修复；
- `docs`：文档；
- `test`：测试；
- `refactor`：重构；
- `perf`：性能改进；
- `build`：构建系统；
- `ci`：持续集成；
- `chore`：其他维护。

示例：

```text
feat(scanner): add incremental root scanning
fix(metadata): prevent stale sidecar overwrite
docs(adr): define external Obsidian references
```

每个提交应保持单一目的，并同时包含该行为需要的测试或文档。禁止在提交中加入真实用户素材、访问令牌、本机缓存和生成的性能大文件。

## 变更流程

1. 从最新 `main` 创建主题分支；
2. 开发代码、测试和文档；
3. 本地执行格式化、静态检查和测试；
4. 检查暂存区，确认没有素材、密钥或缓存；
5. 提交符合规范的信息；
6. 发起评审并附上验收证据；
7. 通过 CI、数据安全和代码评审后合并；
8. 合并后删除主题分支。

## 架构决策要求

以下变更必须先创建或更新 ADR：

- Sidecar 格式、字段含义和迁移规则；
- 原始素材或元数据的任何写入行为；
- 持久化索引或缓存内容；
- 文件移动、删除、复制或重新编码；
- Obsidian 引用语法和本地文件访问方式；
- 权限、安全边界和支持平台；
- 会影响性能基线的核心数据结构。

## 数据安全检查

任何涉及素材或 sidecar 的变更必须验证：

- 操作前后原始素材 SHA-256 不变；
- 删除缓存后能够从素材和 sidecar 重建状态；
- 损坏单个素材或 sidecar 不影响其他素材；
- 失败或崩溃不会产生截断 sidecar；
- 不会访问用户未授权的目录；
- 批量操作能够报告部分失败并安全恢复。

## 评审清单

- [ ] 变更范围与提交说明一致；
- [ ] 新行为包含自动化测试；
- [ ] 错误路径和取消路径已覆盖；
- [ ] 没有引入权威数据库或不可重建状态；
- [ ] 没有隐式移动、复制或修改原始素材；
- [ ] Schema 或协议变更包含 ADR 和迁移说明；
- [ ] 性能敏感变更包含基准对比；
- [ ] 日志不包含访问令牌或不必要的用户路径；
- [ ] 文档与实际行为一致。

## 大文件策略

测试媒体应保持最小化，并优先由确定性脚本生成。不得把以下内容直接提交到 Git：

- 真实用户素材；
- L 数据集或批量生成的夹具；
- 缩略图、索引、性能结果和安装包；
- Node、Rust 或桌面应用构建产物。

如果未来确实需要版本化大型二进制夹具，应先通过 ADR 决定是否启用 Git LFS，以及其存储和下载成本。
